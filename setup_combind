# Run this before anything else

ml load chemistry
ml load schrodinger

unset PYTHONPATH
unset PYTHONHOME
PYTHON=~/miniconda3/bin
[ ":$PATH:" == *":$PYTHON:"* ] || PATH="$PYTHON:$PATH"
export COMBINDHOME=`pwd`

# Set this to your own python site-packages to access your own modules when
# using $SCHRODINGER/run.
export SCHRODINGER_PYTHONPATH=/home/users/jpaggi/miniconda3/lib/python3.6/site-packages/
