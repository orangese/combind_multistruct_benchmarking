# Run this before anything else

ml load chemistry
ml load schrodinger

unset PYTHONPATH
unset PYTHONHOME
PYTHON=/scratch/PI/rondror/combind/miniconda3/bin
[ ":$PATH:" == *":$PYTHON:"* ] || PATH="$PYTHON:$PATH"

export COMBINDHOME=`pwd`

export PYTHONPATH=$PYTHONPATH:$COMBINDHOME/dock:$COMBINDHOME/ifp:$COMBINDHOME/mcss:$COMBINDHOME/score
